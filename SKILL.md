---
name: memory-optimizer
description: 用于优化记忆写入、检查记忆健康状态、执行蒸馏的通用技能。当 Agent 认为需要记住某些信息或用户说"记住/保存"等词时，自动判断写入用户级 Memory 还是项目级 Memory；读取当前项目 MEMORY.md 检查重复、冲突、过时信息；执行每日日志的 30 天留存与蒸馏。This skill should be used when the agent needs to decide where to store a memory, audit memory quality, or distill old daily logs.
agent_created: true
---

# Memory Optimizer

## Overview

持续整理与优化 WorkBuddy 的记忆写入。核心解决两类问题：

1. **写错层级**：Agent 把本应写入用户级（`~/.workbuddy/MEMORY.md`）的信息误写到项目级，或反之。
2. **记忆腐化**：重复、冲突、过时、超出字符上限的记忆条目。

提供三项能力：判断写入目标层、检查记忆健康、执行每日日志蒸馏（30 天留存）。

详细的内存系统结构、路径、字符上限与留存规则见 `references/memory-architecture.md`，
需要时加载到上下文。

## 触发时机

- 用户说"记住 / 保存 / 记一下"等词，或 Agent 自行判断需要持久化某条信息。
- 用户要求"检查记忆 / 记忆健康 / 整理记忆 / 清理日志"。
- 任务收尾需要写记忆，但写入层级不确定。
- 周期性维护或用户主动发起的记忆蒸馏。

## 写前冗余检查（报告去重，最高优先级）

写入任何记忆条目**之前**，先做这一步，以控制记忆总量、避免冗余负担。

**判定规则**：对每一条拟写入的信息，先问——「这条信息是否已经包含在我本次对话已产出的报告 / 文件 / 输出中？」

- **是（报告可查）** → **不重复存储内容**。只在记忆中写入一条**来源指针（source pointer）**：标注该信息的「来源位置 + 内容摘要索引」，便于后续检索。
  示例：
  ```
  - [来源] 受管 Python 在 Windows 下的 venv / 路径坑，详见 2026-09-18 产出报告
    `D:/.../report.md`，检索关键词：venv、Windows 路径、CERT_NONE。
  ```
- **否（报告未含）且具备长期跨会话价值** → 作为**独立条目**，继续下方层级判定后存入。
- **否（报告未含）但仅一次性 / 临时** → 不存。

**来源指针格式约定**：
`- [来源] <一句话内容摘要> | 位置：<绝对路径或 URL> | 关键词：<2-4 个检索词>`
记忆只保留「索引」，详情留在原报告：总量可控、检索可达。

> 注意：来源指针本身也要判层级——跨项目通用的放用户级，仅本项目相关的放项目级或当日日志。

## 能力一：判断写入目标层（写前必做）

完成上方「写前冗余检查」后，对**确需独立存储**的条目，再确定层级。决策树：

1. 信息描述的是**用户本人**或适用于**所有项目**（个人习惯、跨项目约定、安全/权限规则、OS 与工具链事实）？
   -> 写入 **用户级** `~/.workbuddy/MEMORY.md`（上限 4000 字符）。
2. 信息**只与当前项目的代码/上下文相关**（架构决策、技术选型、项目专属约定、本项目经验教训）？
   -> 写入 **项目级** `<workspace>/.workbuddy/memory/MEMORY.md`（上限 3000 字符），
      或按 append-only 规则写入当日 `YYYY-MM-DD.md` 日志。
3. **不确定？** 不要猜测。先问用户：
   "这个规则/信息只用于当前项目，还是所有项目都用？"
   除非用户明确说"所有项目都用"，否则默认理解为"只用于当前项目"。

**禁止写入的层**：
- Layer 1 云记忆（自动注入的个人档案、`conversation_search`）为只读、由服务器管理，永不写入。
- 不要把同一条事实同时存到两个层（跨层重复是缺陷，见健康检查）。

**写入规范**：
- 优先写**来源指针**而非全文复制：若信息已在本轮对话产出的报告中，仅记录来源位置与检索关键词；只有信息不在本次报告且具长期价值时，才写独立条目（见上方「写前冗余检查」）。
- 写前先读取目标文件（及另一层），避免重复与超限。
- 用简洁的条目式（bullet）文字。
- 用户级与项目级各自遵守字符上限；接近 90% 时先压缩再写。
- 当日工作日志只记录有长期价值的内容（建了/修了/重构了什么、选了什么方案、学到什么偏好），
  跳过临时信息（搜索结果、临时路径、工具报错）；日志也优先用来源指针，勿整段复制报告。

## 能力二：检查记忆健康

1. 运行健康检查脚本（确定性扫描，避免每次重新推导日期与逻辑）：
   ```bash
   # <skill_dir> 替换为本技能的安装目录（运行时 Agent 已知该路径，例如 ~/.workbuddy/skills/memory-optimizer）
   python3 "<skill_dir>/scripts/check_memory_health.py" \
       --workspace "<workspace绝对路径>" --home "<用户.workbuddy绝对路径>" \
       --days 30 --reports-dir "<本次对话产出报告所在目录>"
   ```
   说明：`<workspace绝对路径>` 与 `<用户.workbuddy绝对路径>` 按当前环境提供 **Windows 绝对路径（带盘符）**，
   例如 `D:/project/xxx` 与 `C:/Users/你的用户名/.workbuddy`。`python3` 可用 WorkBuddy 受管 Python 或系统 Python 3 替代。
   `--reports-dir`（可选）传入本次对话产出的报告目录后，脚本会标出与报告内容**逐字重复**的记忆条目（应改为来源指针）。
   脚本输出报告，包含：各层字符数 vs 上限、超 30 天的日志候选、层内与跨层重复候选、过时标记、**报告冗余条目**。
   也可加 `--json` 获取结构化结果。

2. 在脚本结果基础上，**人工判断**脚本无法判定的项：
   - **冲突**：两条看起来相似但内容矛盾的条目（脚本只报重复，冲突需 Agent 比对语义）。
   - **过时**：虽无临时标记但已失效的信息（如已变更的版本号、已弃用的流程）。
   - **跨层重复**：同一事实既在用户级又在项目级——保留更通用的一层，删除另一层。
   - **报告冗余**：若提供了 `--reports-dir`，脚本标出与本次报告逐字重复的条目。此类条目应**改为来源指针**（仅留来源位置与关键词），不再全文存储。

3. 向用户指出记忆健康问题清单（重复 / 冲突 / 过时 / 接近上限），并给出优化建议。

## 能力三：每日日志蒸馏（30 天留存）

每日日志保留 30 天。超过 30 天的日志为蒸馏候选，流程：

1. 用健康检查脚本列出超期日志（`old_logs` 段）。
2. 逐篇读取，提取可复用的"经验教训"——非琐碎、可泛化的洞察；跳过临时/一次性内容。
3. 将提炼后的**简短条目**合并进项目 `MEMORY.md`，遵守 3000 字符上限（超限则先压缩既有内容）。
4. **删除原日志前，必须向用户确认无遗漏**：
   "以下 N 篇超 30 天的日志将被提炼后删除，确认没有需要保留的内容吗？"
   仅删除超过 30 天的日志；绝不删除近期日志；未经确认绝不删除。
5. 删除使用文件操作工具（非系统级 `rm -rf`）；删除后向用户报告已清理的日志清单。

## 自动优化执行原则

- 所有**破坏性操作**（删除日志、合并/改写 MEMORY.md）必须先征得用户确认。
- 优化以"去重、消解冲突、清除过时、压缩至上限内"为目标，不改变事实含义。
- 写记忆**优先用来源指针而非全文复制**；仅当信息不在本次报告中且具备长期跨会话价值时，才作为独立条目存储（见「写前冗余检查」）。
- 修改 MEMORY.md 使用 Edit 工具的精确替换；新增当日日志使用 append。
- 完成优化后，给用户一份简洁的变更摘要（哪层、改了什么、删了哪些日志）。

## Resources

- `scripts/check_memory_health.py` — 确定性记忆健康检查（字符上限、超期日志、重复、过时标记）。
- `references/memory-architecture.md` — 三层记忆系统结构、路径、上限、留存规则的完整说明。
